{
  "configurations": [
    {
      "name": "chatgpt-ui-cdk8s",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/.cdk8s",
      "cwd": "${workspaceFolder}/.cdk8s",
      "args": [
        //"--env=Production"
      ],
      "env": {
        "CDK8S_ENVIRONMENT": "Production"
        // "CDK8S_STORAGENAME": "fsdf"
      }
    }
  ]
}
